import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ARUM ALPHA - AI Mineral Estimation System",
  description: "AI-powered mineral estimation and distribution modeling for Bukuru, Jos Plateau, Nigeria. Using machine learning and radiometric data to predict tin (cassiterite) mineralization.",
  keywords: ["mineral estimation", "tin mining", "Jos Plateau", "Nigeria", "machine learning", "geophysics", "radiometric"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
